# 资源仓库共建系统 — 技术开发方案

## 概述

构建四仓库社区共建资源体系，与驾驭工程深度集成。核心设计原则：**一套数据，多端消费**——同一份规范化的 Markdown + YAML 资源文件，既可供驾驭工程 AI 直接检索引用，也可被其他 AI 工具独立读取使用，还能自动生成静态 HTML 页面部署到个人网站展示。

---

## 一、四仓库架构

| 仓库 | 角色 | 面向对象 |
|------|------|----------|
| **prompt-store** | 优秀提示词汇编 | AI 检索 / 用户浏览 / 网站展示 |
| **template-case-store** | 开源项目模板 + 实战案例 | AI 检索 / 用户浏览 / 网站展示 |
| **component-store** | 插件、工具、Skill 索引 | AI 检索 / 用户浏览 / 网站展示 |
| **resource-builder** | 数据处理工具仓库 | 处理三仓库数据，生成 AI 索引 + HTML 页面 |

前三个是**内容仓库**，用户直接贡献内容。第四个是**工具仓库**，负责格式校验、索引生成、HTML 渲染、去重整理，本身也接入 AI 做智能分类和质量评分。

### 1.1 prompt-store（提示词库）

```
prompt-store/
├── README.md                       # 仓库说明 + 使用指南（任何 AI 阅读后即可使用）
├── manifest.json                   # AI 检索一级索引
├── manifest.ai.json                # AI 优化索引（精简字段，降低 token 消耗）
├── prompts/
│   ├── ui-design/
│   │   ├── layout-responsive.md
│   │   └── color-theme-dark.md
│   ├── code-quality/
│   │   ├── react-best-practices.md
│   │   └── naming-conventions.md
│   ├── interaction/
│   └── style-presets/
└── .github/workflows/
    └── pr-auto-label.yml
```

### 1.2 template-case-store（模板案例库）

```
template-case-store/
├── README.md
├── manifest.json
├── manifest.ai.json
├── templates/                      # 可直接套用的工程骨架
│   ├── react-vite-admin/
│   │   └── README.md               # GitHub 地址 + 核心亮点 + 适用场景 + meta.yaml
│   └── vue3-h5-mall/
└── cases/                          # 完整落地项目参考
    ├── dashboard-analytics/
    └── ecommerce-platform/
```

### 1.3 component-store（组件工具库）

```
component-store/
├── README.md
├── manifest.json
├── manifest.ai.json
├── plugins/                        # IDE/编辑器插件索引
├── tools/                          # CLI/开发工具索引
└── skills/                         # Claude Code Skills 集合
```

### 1.4 resource-builder（数据处理工具仓库）

```
resource-builder/
├── README.md
├── package.json
├── src/
│   ├── index.ts                    # 主入口
│   ├── scanner.ts                  # 扫描三仓库，提取 YAML meta + 正文
│   ├── manifestBuilder.ts          # 生成 manifest.json + manifest.ai.json
│   ├── htmlBuilder.ts              # 生成静态 HTML 页面
│   ├── validator.ts                # 元数据格式校验 + 去重检测
│   ├── aiClassifier.ts             # AI 自动分类标签
│   └── linkChecker.ts              # 失效链接检测
├── templates/
│   ├── list.html                   # 列表页模板
│   ├── detail.html                 # 详情页模板
│   └── assets/                     # CSS/JS 静态资源
├── output/                         # 生成的 HTML（可直部署到网站）
│   ├── prompts/
│   │   ├── index.html              # 提示词列表页
│   │   └── detail/
│   │       └── layout-responsive.html
│   ├── templates/
│   │   ├── index.html
│   │   └── detail/
│   └── components/
│       ├── index.html
│       └── detail/
└── .github/workflows/
    └── build-and-deploy.yml        # 定时运行：拉取三仓库 → 重建索引 → 生成 HTML
```

**resource-builder 的职责：**

1. **定时扫描**三仓库最新内容，提取所有 YAML 元数据
2. **生成双索引**：`manifest.json`（完整信息）+ `manifest.ai.json`（AI 精简版，字段更少，token 消耗更低）
3. **AI 自动分类**：对新增或未分类资源，调用 AI 补全 category/tags/use_cases
4. **质量校验**：检查 YAML 格式完整性、去重、失效链接检测
5. **生成 HTML**：渲染三套独立静态页面（列表 + 详情），可直接部署到网站

---

## 二、数据规范 — 一套数据，多端消费

### 2.1 设计原则

每个资源文件是一个自包含的 Markdown 文档，**任何 AI 工具只需阅读一个文件即可理解全部信息**。不需要额外的数据库、不需要专用 SDK、不需要 API。

文件头部 YAML Frontmatter 是结构化元数据，正文是自由内容。人直接阅读 Markdown，AI 解析 YAML 做匹配，HTML 生成器用同样数据渲染网页。

### 2.2 统一文件格式

```yaml
---
name: 通用后台管理系统模板
id: template-admin-react-001
type: template                    # template | case | prompt | plugin | tool | skill
category: 后台管理
subcategory: 数据看板

# 技术标签（AI 检索主键）
tech_stack: [React, TypeScript, Ant Design, Vite]
framework: React
ui_library: Ant Design
build_tool: Vite

# 风格标签
style_tags: [商务稳重, 数据密集, 暗色主题]

# 适用场景
use_cases:
  - 企业后台管理系统
  - 数据统计看板

# 评分与统计（resource-builder 自动更新）
score: 4.7
rating_count: 128
usage_count: 3400
last_verified: 2026-05-20

# 来源
source_url: https://github.com/xxx/awesome-admin
author: 用户昵称
license: MIT
version: 2.1.0
created: 2025-06-01
updated: 2026-05-15

# AI 快速摘要（≤200字，AI 检索时优先匹配此字段）
summary: 基于 React + Ant Design 的企业后台管理系统模板，包含权限管理、数据看板、图表展示等核心功能模块
---

# 通用后台管理系统模板

## 项目地址
https://github.com/xxx/awesome-admin

## 核心亮点
- 完整的 RBAC 权限体系
- 10+ 内置图表组件
- 暗色/亮色双主题切换

## 适用场景
适合快速启动企业级后台管理项目，尤其数据密集型应用。

## 使用方式
git clone 后参照 README 初始化，核心页面可直接复用。
```

### 2.3 双索引体系

**manifest.json** — 完整版，供前端面板和 HTML 生成使用：
```json
{
  "updated": "2026-05-25T10:00:00Z",
  "total": 120,
  "items": [
    {
      "id": "template-admin-react-001",
      "name": "通用后台管理系统模板",
      "type": "template",
      "category": "后台管理",
      "tech_stack": ["React", "TypeScript", "Ant Design"],
      "style_tags": ["商务稳重", "数据密集"],
      "score": 4.7,
      "usage_count": 3400,
      "source_url": "https://github.com/xxx/awesome-admin",
      "summary": "基于 React + Ant Design 的企业后台管理系统模板...",
      "file": "templates/react-vite-admin/README.md"
    }
  ]
}
```

**manifest.ai.json** — 精简版，专供 AI 上下文注入，字段更少，token 消耗降低 60%：
```json
{
  "items": [
    {
      "id": "template-admin-react-001",
      "name": "通用后台管理系统模板",
      "type": "template",
      "category": "后台管理",
      "tech_stack": ["React", "TypeScript", "Ant Design"],
      "style_tags": ["商务稳重", "数据密集"],
      "score": 4.7,
      "source_url": "https://github.com/xxx/awesome-admin",
      "summary": "基于 React + Ant Design 的企业后台管理系统模板..."
    }
  ]
}
```

### 2.4 通用性保障

| 特性 | 实现方式 |
|------|----------|
| **任何 AI 可用** | 标准 Markdown + YAML，clone 即用，无需专用 SDK |
| **人类可读** | 正文为自然语言描述，直接浏览 GitHub 即可理解 |
| **机器可解析** | YAML Frontmatter 结构化字段，`js-yaml` 一行解析 |
| **AI 低 token 消耗** | `manifest.ai.json` 精简版索引，AI 先读索引再按需读详情 |
| **网站直出** | resource-builder 生成纯静态 HTML，拷贝到网站目录即用 |
| **离线可用** | 全部本地文件，无 API 依赖 |

---

## 三、驾驭工程集成（独立模块，最小侵入）

### 3.0 集成原则

**非侵入式扩展**：资源仓库系统作为独立模块存在，不影响现有核心逻辑。驾驭智能体在上下文管线中自动检测资源是否可用——有则注入参考，无则静默跳过。零改动现有文件，全部通过新增文件和管线注册实现。

```
现有项目结构                     新增模块（独立，不修改现有文件）
─────────────                   ─────────────────────────────
electron/                       electron/
├── harnessAgent/               ├── harnessAgent/
│   ├── contextPipeline.ts      │   └── resourceRetriever.ts    ← 新增：AI 检索接口
│   ├── agentLoop.ts            ├── modules/
│   ├── tools.ts                │   └── resourceStore.ts        ← 新增：仓库管理引擎
│   └── ...                     │
├── modules/                    src/
│   └── ...                     └── components/
src/                                └── ResourceMarket/         ← 新增：前端面板（独立 tab）
    └── ...                            ├── ResourceMarket.tsx
                                       ├── ResourceCard.tsx
                                       ├── ContributionPanel.tsx
                                       └── ResourceDetail.tsx
```

**改动清单**：

| 文件 | 改动 | 影响 |
|------|------|------|
| `contextPipeline.ts` | 在 DEFAULT_PIPELINE 数组中追加 `ResourceInjectorProcessor` | 1 行 |
| `tools.ts` | 在工具注册数组中追加 3 个新工具定义 | 3 行 |
| `harnessIpc.ts` | 新增 5 个 IPC handler | 新增函数，不影响现有 |
| `preload.ts` | 暴露 5 个新 API | 新增，不影响现有 |
| `electron.d.ts` | 新增类型声明 | 新增，不影响现有 |
| 其余全部 | **零改动** | — |

### 3.1 新增模块

```
electron/
├── modules/
│   └── resourceStore.ts            # 四仓库管理引擎（独立模块）
├── harnessAgent/
│   └── resourceRetriever.ts        # AI 检索接口（独立文件）
src/components/
└── ResourceMarket/                 # 前端面板（独立 tab 页）
    ├── ResourceMarket.tsx
    ├── ResourceCard.tsx
    ├── ContributionPanel.tsx
    └── ResourceDetail.tsx
```

### 3.2 上下文管线集成（1 行改动）

在 `contextPipeline.ts` 的 `DEFAULT_PIPELINE` 数组末尾追加一个 Processor：

```typescript
// contextPipeline.ts — 仅此一行改动
export const DEFAULT_PIPELINE: ContextProcessor[] = [
  IdentityProcessor,
  TokenBudgetProcessor,
  ContextSelectorProcessor,
  KnowledgeInjectorProcessor,
  CachePrefixerProcessor,
  DynamicInjectorProcessor,
  ResourceInjectorProcessor,  // ← 新增：资源仓库检索注入
]
```

`ResourceInjectorProcessor` 实现为非阻塞、容错设计：

```typescript
// resourceRetriever.ts
export const ResourceInjectorProcessor: ContextProcessor = (view, ctx) => {
  // 如果本地没有资源仓库，静默跳过
  if (!resourceStore.isInitialized()) return view

  // 如果没有当前任务描述，跳过
  if (!ctx.currentTask) return view

  try {
    // 优先读 manifest.ai.json（精简版，低 token 消耗）
    const resources = resourceStore.search(ctx.currentTask, {
      maxResults: 5,
      minScore: 3.0,
      useAiIndex: true,
    })

    if (resources.length > 0) {
      let ref = '\n## 参考资源仓库匹配结果\n'
      for (const r of resources) {
        ref += `- **${r.name}** (${r.type}, ⭐${r.score}) — ${r.summary}\n`
        ref += `  地址: ${r.sourceUrl}\n`
      }
      ref += '\n请在开发时参考以上资源。'
      view.systemPromptParts.push(ref)
    }
  } catch {
    // 任何异常都静默跳过，不影响主流程
  }

  return view
}
```

### 3.3 Agent 工具（仅 tools.ts 注册表追加）

| 工具 | 用途 | 是否影响现有 |
|------|------|------------|
| `search_resources` | 按关键词/类型/标签检索本地四仓库 | 否，新增定义 |
| `add_resource` | 用户自由输入，AI 标准化后写入仓库 | 否，新增定义 |
| `submit_resource` | 将新增/修改的资源提交 PR | 否，新增定义 |

三个工具均为独立的 `AgentTool` 对象，追加到工具注册表即可。

### 3.4 内置标准化插件（独立模块）

用户手动添加资源时，`resourceNormalizer.ts` 负责将任意格式的自由文本转换为标准 Markdown + YAML：

```
用户自由输入（任意格式）
  → AI 提取关键信息（名称/地址/类型/技术栈/用途）
  → 补全 YAML 元数据
  → 生成标准 Markdown 文件
  → 写入本地仓库对应目录
  → 用户可预览、修改、提交
```

```typescript
// resourceNormalizer.ts — 独立模块
interface ResourceNormalizer {
  /** 接收用户自由输入，调用 AI 补全元数据，输出标准化资源文件 */
  normalize(rawInput: string, repo: RepoName): Promise<NormalizedResource>
  batchNormalize(inputs: string[]): Promise<NormalizedResource[]>
}
```

### 3.5 IPC 通道（仅新增，不改现有）

| 通道 | 方向 | 用途 |
|------|------|------|
| `resource:init` | renderer → main | 初始化四仓库（fork + clone） |
| `resource:sync` | renderer → main | 手动同步官方最新 |
| `resource:query` | renderer → main | 前端面板查询资源列表 |
| `resource:add` | renderer → main | 用户添加资源 + AI 标准化 |
| `resource:submit` | renderer → main | 提交贡献 PR |

---

## 四、HTML 网站生成

### 4.1 resource-builder 输出结构

resource-builder 定时拉取三仓库最新内容，生成的 HTML 按以下结构输出：

```
output/
├── prompts/                        # 提示词网站
│   ├── index.html                  # 列表页：卡片式布局，分类筛选，搜索框
│   ├── detail/
│   │   ├── layout-responsive.html  # 详情页：完整 YAML 信息 + 正文
│   │   └── color-theme-dark.html
│   └── assets/                     # 共享静态资源（CSS/JS/图片）
├── templates/                      # 模板案例网站
│   ├── index.html
│   └── detail/
└── components/                     # 组件工具网站
    ├── index.html
    └── detail/
```

### 4.2 页面功能

**列表页 (index.html)**：
- 卡片式布局，每张卡片显示：名称、评分、类型标签、一句话摘要、适用场景
- 顶部筛选栏：分类下拉 + 技术栈标签 + 评分范围 + 关键词搜索
- 排序选项：评分最高、最新加入、最常引用
- 纯静态，零 JS 框架依赖，一个 HTML + 内联 CSS 即可

**详情页 (detail/{name}.html)**：
- 完整 YAML 元数据可视化为表格
- 正文 Markdown 渲染为 HTML
- 底部：相关推荐（同 category / 同 tech_stack 的其他资源）
- 一键复制提示词按钮（针对 prompt 类型）

### 4.3 部署方式

resource-builder 生成纯静态文件，用户只需：
```
将 output/ 目录下的三个文件夹拷贝到网站根目录
→ https://your-site.com/prompts/     → 提示词展示页
→ https://your-site.com/templates/   → 模板案例展示页
→ https://your-site.com/components/  → 组件工具展示页
```

无需数据库、无需后端、无需 API，Nginx/Apache 静态托管即可。

---

## 五、完整生命周期

```
┌─ 首次使用 ──────────────────────────────────────┐
│  1. GitHub OAuth → fork 四仓库到用户 GitHub       │
│  2. clone 到本地: %APPDATA%/dbghf/resources/      │
│  3. 驾驭工程启动 → git pull 同步官方最新            │
└────────────────────────────────────────────────┘
                     │
      ┌──────────────┼──────────────┐
      ▼              ▼              ▼
┌─ AI 检索 ──┐ ┌─ 用户浏览 ──┐ ┌─ 用户添加 ──────┐
│ 任务触发     │ │ 面板查看列表 │ │ 自由输入资源信息  │
│ 读取 manifest│ │ 筛选/搜索    │ │ AI 标准化插件处理 │
│ 匹配资源     │ │ 查看详情     │ │ 写入本地仓库目录   │
│ 注入上下文   │ │ 复制引用     │ │ 用户确认/修改     │
└──────────────┘ └──────────────┘ └────────────────┘
                     │
                     ▼
┌─ 用户贡献回传 ──────────────────────────────────┐
│  1. 贡献面板查看本地变更文件列表                   │
│  2. 勾选 → 填写说明 → AI 补全元数据              │
│  3. git push → GitHub API 发起 PR               │
│  4. CI 自动格式检查 + AI 标签分类                │
│  5. 管理员审核合并                                │
│  6. resource-builder 定时触发：                  │
│     - 扫描三仓库最新内容                          │
│     - 重建 manifest.json + manifest.ai.json      │
│     - 重新生成 HTML 页面                         │
│     - 部署到官方网站                              │
│  7. 所有用户下次启动 → git pull → 获取最新资源     │
└────────────────────────────────────────────────┘
```

---

## 六、可行性评估

### 6.1 技术可行性 ✅ 高

| 模块 | 技术 | 评估 |
|------|------|------|
| Git 操作 | `simple-git` | 成熟封装，支持 clone/pull/push；fork 需 `@octokit/rest` |
| GitHub 集成 | `@octokit/rest` | 官方 SDK，覆盖 fork、PR、认证管理 |
| 本地检索 | `fuse.js` + `js-yaml` | 轻量模糊搜索，适合数千条资源规模 |
| 用户输入标准化 | LLM 调用（现有 Agent 基础设施） | 复用现有 DeepSeek API 通道，零额外成本 |
| HTML 生成 | Handlebars / EJS 模板引擎 | 纯静态渲染，简单可靠 |
| CI 自动化 | GitHub Actions | 定时任务 + PR 事件触发 |
| Token 安全存储 | Electron `safeStorage` | 系统级加密，仅限本地 |

**技术风险与对策：**

| 风险 | 对策 |
|------|------|
| 用户 fork 长期未同步产生冲突 | 提交前强制先 pull；冲突时提供 diff 视图 |
| manifest 与实际文件不同步 | resource-builder 定时重建；提交前自动扫描刷新 |
| GitHub API 限流 | 指数退避重试；操作排队串行化 |
| 用户输入格式千差万别 | AI 标准化插件兜底，无法识别则提示用户手动填写 |
| 跨平台路径 | 统一 `app.getPath('userData')` |

### 6.2 价值评估

| 维度 | 评分 | 说明 |
|------|------|------|
| 开发提效 | ⭐⭐⭐⭐½ | AI 直接检索高质量参考，避免重复造轮子 |
| 知识沉淀 | ⭐⭐⭐⭐ | 结构化、可演进，经验长期积累 |
| 生态自生长 | ⭐⭐⭐½ | PR → 审核 → 同步闭环完整 |
| 与竞品差异化 | ⭐⭐⭐⭐ | 深度集成 AI 工作流 + 自动上下文注入，远超 Awesome 列表 |
| 降低 AI 幻觉 | ⭐⭐⭐⭐ | 可靠参考资源减少凭空捏造 |

### 6.3 风险矩阵

| 风险 | 影响 | 对策 |
|------|------|------|
| 社区冷启动 | 资源少 → 价值低 | 官方预填充 50+ 提示词 + 20+ 模板 |
| 低质量 PR 泛滥 | 维护负担 | CI 格式检查 + 元数据完整性校验 + 评分过滤 |
| GitHub 服务中断 | 同步不可用 | 离线模式可用本地已有资源 |
| 仓库膨胀 | 检索变慢 | 定期去重 + 失效链接清理 + 评分阈值过滤 |

---

## 七、实施计划

### MVP（4 周）

| 周 | 内容 | 交付物 |
|----|------|--------|
| W1 | 四仓库创建 + 目录规范 + YAML 标准 + 50 提示词/20 模板种子 | 四仓库上线，种子内容可读 |
| W2 | resourceStore.ts：initializeRepos + sync + queryResources + resourceNormalizer | 核心引擎可独立验证 |
| W3 | ResourceInjectorProcessor + 4 个 Agent 工具 + 前端面板 | AI 可检索引用；用户可浏览/添加 |
| W4 | resource-builder：manifest 生成 + HTML 渲染 + GitHub Actions 定时构建 | 网站页面可用 |

### 后续迭代

| 阶段 | 内容 |
|------|------|
| P5 | AI 自动分类 + 去重 + 失效链接巡检 |
| P6 | 评分排行榜 + 贡献者统计 + 推荐算法优化 |

---

## 八、技术依赖

| 依赖 | 用途 |
|------|------|
| `simple-git` | Git 操作 |
| `@octokit/rest` | GitHub API |
| `js-yaml` | YAML 解析 |
| `fuse.js` | 本地模糊搜索 |
| `fs-extra` | 文件系统 |
| `handlebars` | HTML 模板渲染 |
| Electron `safeStorage` | Token 加密 |
| GitHub Actions | CI / 定时构建 |

---

## 九、关键设计决策

1. **一套数据，多端消费**：同一份 Markdown + YAML，人看 GitHub、AI 读 YAML、网站用 HTML，互不依赖
2. **双索引**：`manifest.json` 完整版供前端和 HTML 生成，`manifest.ai.json` 精简版降低 AI token 消耗
3. **AI 标准化插件**：用户自由格式输入 → AI 补全元数据 → 规范文件输出，降低参与门槛
4. **静态 HTML 直出**：resource-builder 渲染纯静态页面，零依赖部署
5. **MVP 先验证闭环**：先跑通 prompt-store → AI 检索 → HTML 展示，再扩展其他仓库
6. **用户数据在本地**：所有内容先写入用户本地 fork，确认后再提交 PR，用户对数据有完全控制权
