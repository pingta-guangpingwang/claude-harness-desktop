# 深蓝驾驭工程 — 炫技开发计划

> 底层能力已就绪：Cloud Code 多项目架构、云端 VS Code 终端、多项目窗口切换、同屏联动开发。
> 以下按优先级从易到难排列，每个都是「别人一天、你几分钟」的降维打击能力。

---

## Phase 1: 一键批量拉起 + 终端同步广播

### 1.1 一键批量拉起 N 个 Cloud Code 项目环境

**目标**：点一下，自动同时拉起 5 个不同技术栈项目（PHP/Unity/Node/Python），各自带独立终端、独立工作区，面板随便切。

**实现要点**：
- [ ] `BatchLauncher` 组件：勾选 farm 中多个项目 → 一键启动
- [ ] 并行 `pty:spawn`，每个项目独立 PTY 会话
- [ ] 启动进度条 / 状态卡片（每个项目连接状态实时显示）
- [ ] 预设项目组：保存常用项目组合，一键恢复

### 1.2 多项目终端同步广播命令

**目标**：在一个主终端输命令，所有关联项目终端同时执行。统一安装依赖、统一拉代码、统一打包。

**实现要点**：
- [ ] `BroadcastMode` 开关：主终端输入同步到所有活跃 PTY 会话
- [ ] `electronAPI.ptyBroadcast(command)` — 遍历所有 sessions 写入
- [ ] 广播结果聚合展示：每个项目的输出带颜色标签区分
- [ ] 排除开关：可临时屏蔽某个项目的广播

### 1.3 项目模块一键拆分/合并

**目标**：大项目自动拆成多个子模块独立 Cloud Code 环境，再一键合并回主工程。

**实现要点**：
- [ ] `ModuleSplitter`：读取项目目录结构，智能识别子模块（按文件夹/package.json）
- [ ] 每个子模块独立 `dbht` 项目注册
- [ ] `ModuleMerger`：合并回主工程，保持依赖关系

---

## Phase 2: 每个项目绑定专属开发智能体

### 2.1 专属智能体绑定

**目标**：每个 Cloud Code 项目配一个专属 AI 智能体，负责查报错、改代码、补接口、写文档。切到哪个项目自动激活。

**实现要点**：
- [ ] `AgentProfile` 类型：{ role, expertise, projectPath, systemPrompt, tools[] }
- [ ] 角色预设：代码审查员、接口补全员、文档生成员、Bug 猎人
- [ ] `AgentContext`：当前活跃智能体状态管理
- [ ] PTY 会话绑定智能体 ID，对话自动带上项目上下文
- [ ] 智能体面板：显示当前活跃智能体、快捷切换、对话历史

### 2.2 智能体跨项目协同排错

**目标**：A 项目报错依赖 B 项目接口，两个智能体互相通信，自动定位接口问题、自动改两边代码、自动联调通。

**实现要点**：
- [ ] `CrossProjectAgentBus`：智能体间消息通道
- [ ] 错误传播检测：A 项目 PTY 出现 B 项目相关错误时自动通知
- [ ] 双智能体协同对话视图：左右分栏显示两个智能体的分析过程
- [ ] 自动联调：智能体 A 提出修改 → 智能体 B 验证 → 自动提交

---

## Phase 3: 架构可视化图谱

### 3.1 可视化架构图 + 自动依赖分析

**目标**：所有 Cloud Code 项目、模块、接口、依赖关系画成可视化架构图，点节点直接跳进对应终端和代码，做到看图点哪开发哪。

**实现要点**：
- [ ] `ArchGraph` 组件：Canvas/SVG 渲染项目节点和依赖边
- [ ] `DependencyAnalyzer`：扫描 package.json/composer.json/import 语句自动提取依赖
- [ ] 节点类型：Project、Module、ExternalAPI、Database、Service
- [ ] 双击节点 → 切换到该项目 Chat 面板 + 打开 VSCode 对应文件
- [ ] 拖拽布局、缩放、导出 PNG/SVG
- [ ] 实时状态：节点颜色反映项目 PTY 连接状态、构建状态

---

## Phase 4: 高阶炫技能力

### 4.1 云端多项目实时协同编辑
- [ ] WebSocket 中继服务器
- [ ] 多人光标同步、编辑冲突合并
- [ ] 终端各自独立、代码实时同步

### 4.2 架构工程自动生成完整项目脚手架
- [ ] `ProjectScaffolder`：输入名称、技术栈、模块数
- [ ] 自动生成目录结构 + 配置 + 依赖 + 初始化代码
- [ ] 自动拉起 Cloud Code 环境

### 4.3 终端操作录播复刻
- [ ] PTY 输出录制为操作模板（JSON 格式：{input, expectedOutput, timeout}）
- [ ] `TemplatePlayer`：一键复刻整套操作流程
- [ ] 模板库：环境初始化、部署、打包等常用流程

### 4.4 沙箱安全防护
- [ ] 终端高危命令拦截（rm -rf /、format、del /f 等）
- [ ] 文件防误删：拦截 → 备份 → 告警
- [ ] 沙箱隔离：限制文件访问范围到项目目录

---

## 依赖顺序

```
Phase 1.1 (批量拉起)
  → Phase 1.2 (广播命令) — 依赖多会话
    → Phase 1.3 (拆分/合并) — 依赖批量管理

Phase 1.1 + 当前 Chat 架构
  → Phase 2.1 (专属智能体) — 每个项目已有独立 PTY
    → Phase 2.2 (跨项目协同) — 依赖多智能体

Phase 1.3 + Phase 2.1
  → Phase 3 (架构图谱) — 依赖项目结构和智能体分析

Phase 3
  → Phase 4 (高阶能力)
```

## 建议开发顺序

| 优先级 | 功能 | 预计耗时 | 炫技指数 |
|--------|------|----------|----------|
| **P0** | 一键批量多项目拉起 | 1-2h | ⭐⭐⭐⭐⭐ |
| **P0** | 终端同步广播命令 | 0.5-1h | ⭐⭐⭐⭐⭐ |
| **P1** | 每项目专属开发智能体 | 2-3h | ⭐⭐⭐⭐⭐ |
| **P1** | 架构可视化图谱 | 3-4h | ⭐⭐⭐⭐⭐ |
| P2 | 智能体跨项目协同排错 | 3-4h | ⭐⭐⭐⭐ |
| P2 | 项目模块拆分/合并 | 2-3h | ⭐⭐⭐⭐ |
| P3 | 项目脚手架自动生成 | 2-3h | ⭐⭐⭐⭐ |
| P3 | 终端操作录播复刻 | 3-4h | ⭐⭐⭐ |
| P4 | 云端协同编辑 | 5-8h | ⭐⭐⭐⭐⭐ |
| P4 | 沙箱安全防护 | 2-3h | ⭐⭐⭐ |

---

## 版本规划

| 版本 | 内容 |
|------|------|
| **v3.1 — BroadCast** | Phase 1.1 + 1.2（批量拉起 + 广播） |
| **v3.2 — Agent** | Phase 2.1（专属智能体） |
| **v3.3 — Graph** | Phase 3（架构图谱） |
| **v3.4 — Pro** | Phase 1.3 + 2.2 + 4.2（拆合 + 协同 + 脚手架） |
| **v3.5 — Fortress** | Phase 4.1 + 4.3 + 4.4（协同编辑 + 录播 + 沙箱） |
