# 深蓝生态开发计划 — DBGHF (驾驭工程)

> **项目路径**: `H:\DeepBlueGodHarnessFarm`
> **角色**: 深蓝生态总控台 — Claude Code 驾驭智能体的统一入口
> **技术栈**: Electron 28 / React 19 / TypeScript / node-pty / xterm.js
> **当前版本**: v2.5.0
> **目标版本**: v3.0.0 — 以 Claude Code 为唯一驾驭智能体的项目工厂

---

## 1. 定位转变

### 原定位
独立 Electron 桌面平台，自研 CLI/工作流/角色系统/插件管理

### 新定位 (v3.0)
**Claude Code 的总控台**。不做 Agent 引擎，只做:
- 多项目管理 (DBHT 驱动)
- VSCode 启动器
- PTY 终端代理 (捕获 Claude Code I/O)
- 对话面板 (在驾驭工程内直接对话)
- 交互记录 / Agent 记忆 / Skill 管理

```
用户在驾驭工程:
  选项目 → 一键启动 → VSCode 打开 + Claude Code 在终端就绪
  → 在驾驭工程对话面板直接跟 Claude 对话
  → 交互记录自动保存
  → DBHT 自动快照
```

---

## 2. 当前状态 (v2.5.0)

### 保留的能力
- **项目管理 UI**: 项目列表/卡片/排序/星级/备注 → **核心，继续用**
- **知识图谱查看器**: 建筑隐喻可视化 → 保留
- **向量搜索面板**: 语义搜索 → 保留
- **健康仪表盘**: 代码质量 → 保留
- **工作流编辑器**: SVG Canvas DAG → 保留 (未来可对接多 Agent)
- **CLI 命令系统**: → 简化为 DBHT 命令包装
- **i18n 双语**: → 保留

### 移除/精简的能力
- 系统托盘 / 全局快捷键 / 开机自启 → shenlanAI 管
- 角色身份体系 / 技能碎片 → Claude Code 自带 CLAUDE.md 规则替代
- 插件市场 → shenlanAI 管
- AI 模型网关 → Claude Code 自带多模型
- Electron IPC 模块精简: 只保留 DBHT / 文件系统 / 终端管理

---

## 3. v3.0 — Claude Code 总控台 (T+3周)

### 模块一: PTY 终端代理 (P0)

驾驭工程通过 node-pty 管理 Claude Code 进程，stdin/stdout 双向代理。

```
Electron Main Process:
  pty.spawn("claude", [], { cwd: projectPath, env: process.env })
       │
       ├─ stdin.write(userInput)      ← 用户在驾驭工程打字
       └─ stdout.on('data', chunk)    → 实时推送到对话面板
```

| # | 任务 | 说明 | 预估 |
|---|------|------|------|
| 1 | node-pty 集成 | Electron main process 管理 PTY 进程 | 2d |
| 2 | PTY IPC 通道 | preload 暴露 pty:write / pty:data / pty:resize | 1d |
| 3 | Claude Code 启动流程 | spawn claude → 检测就绪 (prompt 出现) → 通知 UI | 1d |
| 4 | 进程生命周期 | 启动/重启/终止/异常退出处理 | 1d |

### 模块二: 对话面板 (P0)

驾驭工程内嵌的聊天 UI，渲染 Claude Code 的终端输出。

| # | 任务 | 说明 | 预估 |
|---|------|------|------|
| 5 | ChatPanel 组件 | 对话气泡列表 + 输入框 + 发送按钮 | 2d |
| 6 | ANSI 输出渲染 | Claude TUI 的 ANSI 转义序列 → React 组件 | 2d |
| 7 | 流式渲染 | 实时追加 Claude 输出，自动滚动 | 1d |
| 8 | 输入代理 | 用户输入 → PTY stdin, 支持多行/粘贴 | 1d |

### 模块三: 项目启动器 (P0)

| # | 任务 | 说明 | 预估 |
|---|------|------|------|
| 9 | VSCode 启动 | `code <project-path>` 打开项目 | 0.5d |
| 10 | DBHT 集成 | 读取项目注册表，显示项目状态 | 1d |
| 11 | 一键启动流程 | 选项目 → 开 VSCode → 开终端 → 启 Claude | 1d |

### 模块四: 交互记录 (P1)

| # | 任务 | 说明 | 预估 |
|---|------|------|------|
| 12 | 会话持久化 | 对话 JSON 存储到项目 `.dbvs/chat/` | 1d |
| 13 | 历史回放 | 查看/搜索/回放历史对话 | 2d |
| 14 | 会话列表 | 按时间/项目/标签筛选 | 1d |

### 模块五: Agent 记忆 & Skill (P1)

| # | 任务 | 说明 | 预估 |
|---|------|------|------|
| 15 | CLAUDE.md 管理 | 编辑/预览项目的 CLAUDE.md (Claude 规则文件) | 1d |
| 16 | Skill 管理 | 管理 `.claude/skills/` 目录，安装/启用/禁用 | 1d |
| 17 | 记忆摘要 | 从历史对话中提取关键决策，生成项目记忆 | 2d |

### 模块六: Middleware Box 对接 (P1)

| # | 任务 | 说明 | 预估 |
|---|------|------|------|
| 18 | 审计事件上报 | 每次对话回合 POST 到 Middleware Box | 1d |
| 19 | Agent 画像查询 | GET /api/agents/:id 展示 Agent 使用统计 | 1d |

---

## 4. 新增/修改文件

```
H:\DeepBlueGodHarnessFarm\
├── electron/
│   ├── main.ts                      # 精简: 移除托盘/热键/多窗口
│   ├── preload.ts                   # 新增 PTY IPC 通道 + 精简
│   └── modules/
│       ├── ptyManager.ts            # ★ 新增: PTY 进程管理
│       ├── dbhtIpc.ts               # 保留: DBHT 集成
│       ├── middlewareBridge.ts       # 改造: 真实 HTTP 调用 (当前是 mock)
│       └── (删除) modelGateway.ts   # Claude Code 自带多模型
│       └── (删除) roleIpc.ts        # CLAUDE.md 替代
│       └── (删除) pluginIpc.ts      # shenlanAI 管
├── src/
│   ├── components/
│   │   ├── Chat/                    # ★ 新增
│   │   │   ├── ChatPanel.tsx        #   对话面板
│   │   │   ├── ChatBubble.tsx       #   对话气泡
│   │   │   ├── ChatInput.tsx        #   输入框
│   │   │   └── AnsiRenderer.tsx     #   ANSI → React
│   │   ├── Session/                 # ★ 新增
│   │   │   ├── SessionList.tsx      #   会话列表
│   │   │   ├── SessionViewer.tsx    #   会话回放
│   │   │   └── MemoryPanel.tsx      #   Agent 记忆
│   │   ├── Skill/                   # ★ 新增
│   │   │   └── SkillManager.tsx     #   Skill 管理
│   │   ├── HorseFarm/               # 保留 + 精简
│   │   │   ├── HorseFarm.tsx        #   主面板 (新增对话视图)
│   │   │   ├── ProjectList.tsx      #   项目列表
│   │   │   └── ...
│   │   └── Shared/
│   │       └── VirtualList.tsx      # 保留
│   └── context/
│       └── ChatContext.tsx          # ★ 新增: 对话状态管理
```

---

## 5. 技术决策

| 决策 | 选择 | 原因 |
|------|------|------|
| 终端方案 | node-pty | 完整伪终端，支持 ANSI，Claude Code TUI 兼容 |
| ANSI 渲染 | 自建解析器 (轻量) | Claude 输出主要是颜色+进度，不需要完整 xterm.js |
| 会话存储 | JSON 文件 + DBHT 快照 | 简单可靠，可版本化 |
| 对话 UI | 自建 React 组件 | 可控，可嵌入记忆/Skill 面板 |
| VSCode 启动 | `code` CLI | 标准跨平台方式 |

---

## 6. v3.1 — 监控增强 (T+2周)

| # | 任务 | 说明 |
|---|------|------|
| 1 | 终端进度解析 | 检测 Claude 工具调用 (Read/Write/Bash) → 进度条 |
| 2 | 多会话并行 | 同时管理多个项目的 Claude Code 会话 |
| 3 | 会话对比 | 对比两次 AI 会话的代码变更 |
| 4 | 一键回滚 | 从对话面板直接回滚某次 AI 会话 |

---

## 7. 废弃清单 (v2.5 → v3.0 删除)

| 模块 | 原因 |
|------|------|
| `modelGateway.ts` | Claude Code 自带多模型支持 |
| `roleIpc.ts` + `src/roles/` | CLAUDE.md 规则替代角色门控 |
| `pluginIpc.ts` + `PluginStore.tsx` | 插件管理迁移到 shenlanAI |
| `cliIpc.ts` + CLI 子系统 | 仅保留 DBHT 命令包装 |
| `workflowIpc.ts` + WorkflowCanvas | 保留但降级为 P2 |
| `trayManager.d.ts` / `hotkeys.d.ts` | 已迁移到 shenlanAI |
| `knowledgeGraphIpc.ts` | DBHT 直接 CLI 调用 |

---

> **进度追踪**: 底座项目 [深蓝生态开发进度](h:\YunSvn\webRes\shenlangod\shenlanai\深蓝生态开发进度.md)
